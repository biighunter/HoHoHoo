version: '3'

services:
  Taco-server:    
    image: harbor.shomamamama.com/hohohoo/taco:latest
    container_name: taco
    expose:
      - 80
      - 443
    networks:
      - Ho
networks:
  Ho:
    external: true
